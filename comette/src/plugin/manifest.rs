pub use comette_manifest::{
    ConfigBool, ConfigFilePath, ConfigFolderPath, ConfigInt, ConfigList, ConfigMap, ConfigSchema,
    ConfigSelection, ConfigStr, ConfigStruct, ConfigType, PluginManifest,
};
